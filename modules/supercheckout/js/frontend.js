(function($) {

	BACheetahSupercheckout = function( node ){
		/*
		this.iframe = $(".ba-node-" + node + ".ba-module__supercheckout iframe")[0];
		var vm = this;

		window.setInterval(function(){
		 	if (vm.iframe) {
		 		vm.iframe.height = vm.iframe.contentWindow.document.body.scrollHeight + "px";
		 	}
		}, 250);
		*/
	};

})(jQuery);
